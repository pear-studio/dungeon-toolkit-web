## AI规范与基础建设规则体系实施计划

### 一、规范文件创建

1. **docs/conventions/ai-usage.md** - AI使用标准

   * AI使用场景定义（适合AI vs 不适合AI的任务）

   * 上下文管理规范（会话信息维护）

   * 项目规则引用方式

   * AI输出验证流程

2. **docs/conventions/data-sources.md**（更新）

   * 强化核心原则：必须从 `rules_source/` 读取数据

   * 数据优先级：rules\_source > 官方SRD > Open5e API

   * 禁止行为：记忆生成、凭空编造、第三方复制

   * Slug命名规范：kebab-case + 翻译转换

### 二、Python数据读取脚本（新增）

在 `backend/data/parsers/` 创建查询脚本：创建一个通用的查询脚本

**AI使用方式**：直接运行脚本快速获取数据，而非每次读取HTML

### 三、TRAE项目规则配置

在 `.trae/rule/` 创建：

| 规则文件                       | 生效方式 | 作用范围          |
| -------------------------- | ---- | ------------- |
| `ai-usage.md`              | 始终生效 | 所有对话          |
| `data-sources.md`          | 指定文件 | \*.json       |
| `code-style-python.md`     | 指定文件 | \*.py         |
| `code-style-typescript.md` | 指定文件 | \*.ts, \*.tsx |

### 四、文件更新

* `docs/conventions/README.md` - 新增规范索引


# Django 密钥（生产环境必须替换为随机长字符串）
# 生成命令：python3 -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=dev-secret-key-change-me-in-production

# 调试模式（生产环境必须设为 False）
DEBUG=False

# 允许访问的域名或 IP，多个用逗号分隔
# 开发环境填 localhost，生产环境填服务器 IP 或域名
ALLOWED_HOSTS=localhost,127.0.0.1

# 数据库配置（必须与 docker-compose.yml 中 db 服务的配置保持一致）
DB_NAME=dungeon_toolkit
DB_USER=dungeon_user
DB_PASSWORD=dungeon_pass_2024
DB_HOST=db
DB_PORT=5432

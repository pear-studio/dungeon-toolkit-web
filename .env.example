# ================================================================
# Dungeon Toolkit 环境变量配置
# 使用方法：cp .env.example .env  然后填写下方变量
# 注意：.env 文件已加入 .gitignore，不会被提交到 git
# ================================================================

# ----------------------------------------------------------------
# Django 核心配置
# ----------------------------------------------------------------

# 密钥：生产环境必须改成随机字符串，可用下方命令生成：
#   python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-secret-key-change-this-in-production

# 允许访问的域名/IP，多个用逗号分隔
# 示例：ALLOWED_HOSTS=dungeon-toolkit.yourdomain.com,123.456.789.0
ALLOWED_HOSTS=localhost,127.0.0.1

# 允许跨域的前端地址（与你的域名保持一致）
# 示例：CORS_ALLOWED_ORIGINS=http://dungeon-toolkit.yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost

# ----------------------------------------------------------------
# 数据库配置
# ----------------------------------------------------------------

DB_NAME=dungeon_toolkit
DB_USER=dungeon_toolkit

# 数据库密码：生产环境请设置强密码！
DB_PASSWORD=change-this-strong-password

# Docker 内部通信，不要改
DB_HOST=db
DB_PORT=5432

# ----------------------------------------------------------------
# AI 功能配置（可选，不填则 AI 功能不可用）
# ----------------------------------------------------------------

# 支持的提供商：openai / claude / qianwen
AI_PROVIDER=

# OpenAI 或兼容接口（如 ChatGLM、通义千问 OpenAI 兼容模式）
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o-mini